<div class="landing-page {{contentClass}}">
  {{#if showBanner}}
    <div class="launch-landing">
      <a name="banner"></a>
      {{site-banner}}
    </div>
  {{/if}}
  {{outlet 'content'}}
  <div class="centered">
    {{landing-footer}}
  </div>
</div>
