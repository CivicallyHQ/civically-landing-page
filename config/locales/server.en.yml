en:
  contact_mailer:
    title: "New Contact"
    subject_template: "New %{type} contact from %{name}"
    text_body_template: |
      type: %{type}
      name: %{name}
      email: %{email}
      phone: %{phone}
      institution: %{institution}
      position: %{position}
      message: %{message}

  launch_mailer:
    title: "Early Access"
    subject_template: "Early Access for %{email}"
    text_body_template: |
      email: %{email}

  site_settings:
    landing_contact_email: "Email to send landing page contacts to."
    landing_contact_phone_enabled: "Add a phone input in the landing contact form."
    landing_visible_locales: "Locales visible on the landing page."
